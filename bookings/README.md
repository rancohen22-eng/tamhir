# מערכת תיעוד הזמנות טיסות · ארקיע

מערכת ענן **רב-משתמשית** לניהול ותיעוד מחזור החיים של הזמנות טיסה בארגון — מהייזום, דרך הצעת מחיר מהסוכן ואישור הגורם המאשר, ועד לכרטוס ותיעוד המסמכים. במיתוג ארקיע, דו-לשונית (עברית/אנגלית), עם התראות, מעקב מלא ומסך כספים.

רצה על **Oracle Autonomous Database** (Always Free): סכמה ייעודית (`ARKIA`) עם לוגיקת PL/SQL, שכבת **REST דרך ORDS**, ואפליקציית ווב (SPA) המוגשת ישירות מ-ORDS. פריסה **אוטומטית** (CI/CD) בכל שינוי.

## 🔗 גישה
- **האפליקציה:** `/ords/arkia/api/app` (על מארח ה-ADB שלכם).
- **התחברות דמו** (סיסמה `Arkia2026!` — לשנות!): `admin` · `initiator_acmi` · `approver_acmi` · `finance` · `teltos` (סוכן).

---

## ארכיטקטורה
```
דפדפן ── HTTPS ──▶  ORDS  /ords/arkia/api/*
                     │
   ┌─────────────────┼───────────────────────────┐
   │  app  (SPA HTML, מוגש מ-media handler)        │
   │  assets/arkia|teltos  (לוגואים)              │
   │  login/bootstrap/bookings/.../admin  (JSON)  │
   └─────────────────┬───────────────────────────┘
                     ▼
        api_pkg  (JSON + אימות טוקן)  ──▶  booking_pkg (workflow)
                     ▼
        טבלאות ARKIA (הזמנות, קבצים, אודיט, התראות, ...)
```

## יכולות
- מחזור חיים מלא: ייזום → הצעת מחיר (+צילום) → אישור/דחייה → כרטוס (PNR+מזהה+מסמך), כולל **ביטול דו-שלבי**.
- **תפקידים** ואכיפה בשרת: אדמין/מזמין/מאשר/סוכן/כספים; הסוכן רואה רק בקשות שהופנו אליו, מזמין/מאשר — הזמנות המחלקה.
- **קבצים** כ-BLOB (העלאה/הורדה), **פעמון התראות** (מונה לא-נקראו), **ציר-זמן** "מי/מתי/מה".
- **מסך כספים** (סיכומים + ייצוא CSV/Excel) ו**מסכי ניהול** (הוספת מחלקות/משתמשים/סוכנים + שינוי סיסמה) — הכל באפליקציה, ללא SQL.
- **דו-לשוני** עברית/אנגלית עם החלפת דגל 🇮🇱/🇺🇸 (RTL/LTR).
- **מיתוג** ארקיע + לוגו הסוכן טלטוס (co-branding).
- **התראות מייל** (Gmail SMTP) — אופציונלי, ראו `deploy/email_setup.sql`.

## מבנה התיקייה
```
bookings/
├── README.md               ← מסמך זה
├── db/                      ← שכבת בסיס הנתונים (מותקנת פעם אחת)
│   ├── 01_tables.sql        · טבלאות
│   ├── 02_seed.sql          · נתוני יסוד + משתמשי דמו + הסוכן טלטוס
│   ├── 03_booking_pkg.sql   · חבילת workflow (change_status, מעברים, אימות, מייל)
│   ├── 04_views.sql         · Views לדוחות/כספים/ציר-זמן
│   ├── 05_grants.sql        · הרשאות (הערות)
│   └── install_all.sql      · התקנה מאוחדת (01–04)
├── api/                     ← שכבת REST + UI  (נפרסת אוטומטית ב-CI)
│   ├── install_api.sql      · api_sessions + api_pkg + מודול ORDS (endpoints)
│   └── install_ui.sql       · מגיש את ה-SPA והלוגואים מ-ORDS (נוצר אוטומטית מ-app/)
├── app/
│   └── index.html           ← אפליקציית ה-SPA (מקור; מוטמע ל-install_ui.sql)
├── deploy/                  ← פריסה + מייל
│   ├── README.md            · הגדרת ה-CI/CD (Wallet + Secrets)
│   ├── email_setup.sql      · הגדרת Gmail SMTP (ADMIN, חד-פעמי)
│   └── email_test.sql       · בדיקת שליחה
├── assets/                  ← arkia-logo.svg · teltos-logo.jpeg
└── apex/BUILD_GUIDE.md      ← מסלול חלופי: בניית אפליקציה ב-APEX Builder (לא בשימוש)
```

## התקנה (תמצית)
1. **בסיס נתונים** (פעם אחת, כ-`ARKIA`): הריצו את `db/install_all.sql`.
2. **REST + UI**: הריצו `api/install_api.sql` ואז `api/install_ui.sql`.
3. **גישה**: פתחו `/ords/arkia/api/app`.
4. **פריסה אוטומטית**: לאחר הגדרת ה-CI (ראו `deploy/README.md`), כל push לקבצי `api/**`/`app/**` מתקין את השינוי אוטומטית — ללא הרצה ידנית.

> הערה: `bookings/index.html` (בשורש התיקייה) הוא **גרסת MVP** בקובץ יחיד עם `localStorage` — זהה בפשטות למערכות הסטטיות, אך **ללא שיתוף בין משתמשים/מכשירים**. הגרסה הרב-משתמשית היא זו שב-`app/` + `api/`.

## אבטחה — לפני שימוש אמיתי
- **החליפו את סיסמאות הדמו** (`Arkia2026!`) — דרך מסך הניהול באפליקציה, או `booking_pkg.set_password`.
- הגדירו כתובת שולח מייל אמיתית (מוגדר: `arkiapnr@gmail.com`).
- שקלו הידוק CORS (כרגע `*` — האפליקציה מוגשת מאותו מקור) והעברת הטוקן לכותרת.
- סיסמאות נשמרות כ-hash (SHA-256) עם salt ייחודי לכל משתמש.
